import React, { FC } from "react";

const Navigation: FC = () => {
  return (
    <button className="text-white text-xl p-2 py-1 hover:bg-white/10 rounded-lg transition-colors">
      <i className="ri-menu-2-fill"></i>
    </button>
  );
};

export default Navigation;
